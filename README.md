To View available Endpoints: http://127.0.0.1:8000/docs

1. pipenv shell(To activate subshell environment)
2. pipenv install(To insall dependencies)
3. uvicorn crud:app --reload(To start app on local server)


